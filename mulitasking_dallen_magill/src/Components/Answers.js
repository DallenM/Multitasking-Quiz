function Answers(props) {
    const answerOptions = [];
    let i;
    for (i = 0; i < props.questions.length; i++) {
        //console.log(props.questions[i].answerOptions)
    }

    return(answerOptions);
}
export default Answers;
